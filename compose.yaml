services:
  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_NAME:-premier_league_predictions_dev}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-premier_league_predictions_dev}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    build:
      context: ./backend
    environment: &backend_environment
      NODE_ENV: development
      PORT: 3000
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env}
      DB_HOST: database
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-premier_league_predictions_dev}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
      # Football data sync. Disabled by default: the scheduled jobs only run
      # when FOOTBALL_SYNC_ENABLED is true, the CLI works either way.
      FOOTBALL_SYNC_ENABLED: ${FOOTBALL_SYNC_ENABLED:-false}
      FOOTBALL_API_KEY: ${FOOTBALL_API_KEY:-}
      FOOTBALL_COMPETITION: ${FOOTBALL_COMPETITION:-PL}
      FOOTBALL_SEASON: ${FOOTBALL_SEASON:-}
      FOOTBALL_SYNC_GAMEWEEK_HORIZON: ${FOOTBALL_SYNC_GAMEWEEK_HORIZON:-3}
      FOOTBALL_SYNC_RESULTS_INTERVAL_SECONDS: ${FOOTBALL_SYNC_RESULTS_INTERVAL_SECONDS:-300}
      FOOTBALL_FINISH_WINDOW_START_MINUTES: ${FOOTBALL_FINISH_WINDOW_START_MINUTES:-95}
      FOOTBALL_FINISH_WINDOW_END_MINUTES: ${FOOTBALL_FINISH_WINDOW_END_MINUTES:-240}
    command: sh -c "npm run migrate && npm run db:seed"
    # Same mount as the backend so migrations added after the image was built
    # are always visible; without this the service runs a stale copy.
    volumes:
      - ./backend:/app
      - backend_node_modules:/app/node_modules
    depends_on:
      database:
        condition: service_healthy
    restart: "no"

  backend:
    build:
      context: ./backend
    environment: *backend_environment
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - backend_node_modules:/app/node_modules
    depends_on:
      database:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://localhost:3000/api/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
  backend_node_modules:
  frontend_node_modules:
