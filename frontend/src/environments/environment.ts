export const environment = {
  production: false,
  apiUrl: "http://localhost:3000/api",
  // Add other environment-specific settings here
};

console.log("🟢 DEV Environment loaded:", environment);
